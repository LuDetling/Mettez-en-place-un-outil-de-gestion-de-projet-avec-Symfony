{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
	<section id="fleet">
		<h2>Découvrez la flotte de véhicules chez Véhicloc</h2>
		<p>Que vous ayez besoin d'une petite citadine agile, d'une berline élégante ou même d'un SUV spacieux, nous avons une variété de véhicules  pour répondre à vos exigences.</p>
		<div class="fleet-list">
			{% for car in cars %}
				<article>
					<div class="mini-car-title">
						<a href="{{ path('showcar', {id: car.id}) }}">
							<h3>{{ car.name }}</h3>
							<p>ou similaire | Saint-Étienne</p>
						</a>
						<div>
							<form action="{{ path('deleteCar', {id: car.id}) }}" method="POST">
								<input type="hidden" name="_method" value="DELETE">
								<button type="submit"><img src="/assets/icons/trash.png" alt="Supprimer"/></button>
							</form>
						</div>
					</div>
					<div class="mini-car-icons">
						<div>
							<img src="/assets/icons/persons.png" alt="4 places"/>
							<p>{{ car.seat }}
								places</p>
						</div>
						<div>
							<img src="/assets/icons/motor.png" alt="Manuelle"/>
							<p>{{ car.gearbox }}</p>
						</div>
						<div>
							<img src="/assets/icons/km.png" alt="km illimités"/>
							<p>Km illimités</p>
						</div>
						<div>
							<img src="/assets/icons/plus.png" alt="PLus"/>
							<p>Plus</p>
						</div>
					</div>
					<div class="mini-car-price">
						<p class="price-per-day">{{ car.prixDay }}
							€ / jour</p>
						<p class="price-per-month">{{ car.prixLocation|number_format(2) }}
							€ / mois</p>
					</div>
				</article>
			{% endfor %}

		</div>
	</section>
{% endblock %}
